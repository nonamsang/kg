<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moodshop.kokone.dao.WishListDAO">

    <select id="getWishlistCountByProduct" resultType="int">
        SELECT COUNT(*) FROM WISHLIST WHERE product_id = #{productId}
    </select>

    <select id="getWishlistByUser" parameterType="string" resultType="ProductVO">
        SELECT p.product_id, p.product_name, p.product_price, p.product_image
        FROM product p
        JOIN WISHLIST w ON p.product_id = w.product_id
        WHERE w.user_id = #{userId}
    </select>

    <select id="checkIfWished" resultType="int">
        SELECT COUNT(*) FROM WISHLIST WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <insert id="insertWishlist">
        INSERT INTO WISHLIST (user_id, product_id) VALUES (#{userId}, #{productId})
    </insert>

    <delete id="deleteWishlist">
        DELETE FROM WISHLIST WHERE user_id = #{userId} AND product_id = #{productId}
    </delete>
    
    <select id="countWishlistByProductId" resultType="int">
        SELECT COUNT(*) FROM WISHLIST WHERE product_id = #{productId}
    </select>

</mapper>