<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fintecho.littleforest.mapper.admin.AdminProductMapper">


	<select id = "getAllProduct">
		SELECT * FROM product_table WHERE merchants_id = 4 order by id
	</select>
	<insert id="insertProduct" parameterType="com.fintecho.littleforest.dto.ProductDTO">
		INSERT INTO product_table
        (id, merchants_id, name, category, image_path, price, carbon_effect, description)
   		 VALUES (product_seq.nextval, 4, #{name}, #{category}, #{image_Path}, #{price}, 1.0, #{des})
	</insert>
  
	<update id="updateProduct" parameterType="com.fintecho.littleforest.vo.ProductVO">
	  UPDATE product_table
	     SET name        = #{name},
	         category    = #{category},
	         price       = #{price},
	         description = #{description}
	   WHERE id = #{id}
	</update>
	
	<delete id = "deleteProduct" parameterType="com.fintecho.littleforest.vo.ProductVO">
	
		DELETE FROM product_table WHERE id = #{id} 
	</delete>

</mapper>